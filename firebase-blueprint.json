{
  "entities": {
    "MarketSnapshot": {
      "title": "Market Snapshot",
      "description": "Daily quantitative snapshot of global market indices and commodities.",
      "type": "object",
      "properties": {
        "date": { "type": "string", "description": "ISO date string for the snapshot." },
        "assets": { 
          "type": "array", 
          "description": "List of assets with their metrics."
        },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["date", "assets", "updatedAt"]
    }
  },
  "firestore": {
    "/snapshots/{date}": {
      "schema": "MarketSnapshot",
      "description": "Stores daily snapshots of all tracked assets."
    }
  }
}
